import React from "react";
import { useDispatch } from "react-redux";

const Cart = () => {
  const dispatch = useDispatch();
  return (
    <div>
      <h2>Cart</h2>
    </div>
  );
};

export default Cart;
